# Default values for gitops-audit

image:
  repository: koolinst/gitops-audit
  tag: "7fbf0d1a704ce5eaca08de5b7df95c13c4b5aa69"
  pullPolicy: IfNotPresent

watcher:
  enabled: true
  replicaCount: 1
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 200m
      memory: 256Mi

api:
  enabled: true
  replicaCount: 1
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 200m
      memory: 256Mi

service:
  type: ClusterIP
  port: 8000

config:
  logLevel: INFO
  prometheusUrl: "http://prometheus-operated:9090"
  argocdUrl: "http://argocd-server.argocd.svc.cluster.local"

secrets:
  githubToken: ""
  slackWebhookUrl: ""
  databaseUrl: "postgresql+asyncpg://postgres:password@postgres:5432/gitops_audit"

serviceAccount:
  create: true
  name: gitops-audit

argocd:
  namespace: argocd
